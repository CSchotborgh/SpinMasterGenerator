import type { Express } from "express";

export function registerRoutes(app: Express) {
  // For this application, all logic is handled client-side
  // If we need to add server-side features like saving wheel configurations,
  // we would add the routes here
}
